/// Canonical string paths for all GoRouter routes.
class RoutesPath {
  static const home = '/';
  static const order = '/order';
}

/// Named route identifiers used with `pushNamed` and friends.
class RouteNames {
  static const home = 'home';
  static const order = 'order';
}
